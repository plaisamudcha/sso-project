info:
  name: Get all oauth-client
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/oauth-client"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
