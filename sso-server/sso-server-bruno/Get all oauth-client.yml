info:
  name: Get all oauth-client
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/oauth-client"
  headers:
    - name: x-admin-api-key
      value: "{{x-admin-api-key}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
