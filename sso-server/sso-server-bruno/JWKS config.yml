info:
  name: JWKS config
  type: http
  seq: 5

http:
  method: GET
  url: "{{BASE_URL}}/.well-known/jwks.json"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
