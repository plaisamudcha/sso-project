info:
  name: openid-configuration
  type: http
  seq: 4

http:
  method: GET
  url: "{{BASE_URL}}/.well-known/openid-configuration"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
