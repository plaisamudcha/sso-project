info:
  name: Register user
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASE_URL}}/register"
  body:
    type: json
    data: |-
      {
        "email": "plai@hotmail.com",
        "password": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
